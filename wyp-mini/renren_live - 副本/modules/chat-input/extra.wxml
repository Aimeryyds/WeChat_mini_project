<template name="chat-extra-function-part">
    <view class="extra-super">
        <view bindtap="chatInputExtraItemClickEvent" class="flex-column" data-index="{{index}}" style="width: 50%" wx:for="{{chat$input$extra$arr}}" wx:key="{{index}}">
            <image class="extra-image-size" src="./../../images/chat/extra/{{item.picName}}.png"></image>
            <text class="extra-text-size">{{item.description}}</text>
        </view>
    </view>
</template>
