<view class="dengji_icon" level="{{level}}" style="background: {{backgroundcolor}}" typeStr="{{typeStr}}">
    <image class="dengji_img" src="{{iconUrl}}"></image>
    <text class="dengji_txt">{{level}}</text>
</view>
