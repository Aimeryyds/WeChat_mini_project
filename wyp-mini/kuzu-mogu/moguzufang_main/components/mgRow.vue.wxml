<template name="3a5abe60">
    <view bindtap="handleProxy" class="_view 3a5abe60 mg-flex mg-row" data-comkey="{{$k}}" data-eventid="{{'3a5abe60-0'}}" wx:if="{{!params.hidden}}">
        <view class="_view 3a5abe60 mg-row-title">{{title}}</view>
        <view class="_view 3a5abe60 mg-flex-col mg-ellipsis mg-row-desc">{{desc}}</view>
        <view class="_view 3a5abe60 mgic-angle-r" wx:if="{{hasClick}}"></view>
    </view>
</template>
