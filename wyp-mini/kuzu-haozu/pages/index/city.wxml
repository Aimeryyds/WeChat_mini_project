<view class="cityBox">热门城市</view>
<view class="cityList">
    <navigator class="{{domain==item.domain?'cur':''}}" openType="reLaunch" url="index?domain={{item.domain}}" wx:for="{{citys}}" wx:key="id">{{item.name}}</navigator>
</view>
