var array = require('p_./pages/vant/wxs/array.wxs')();

function isActive(activeList, itemId) {
    if (array.isArray(activeList)) {
        return (activeList.indexOf(itemId) > -1)
    };
    return (activeList === itemId)
};
module.exports.isActive = isActive;