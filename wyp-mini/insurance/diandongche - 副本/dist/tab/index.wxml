<view bindtap="onTap" class="wux-class {{classes.wrap}}">
    <view class="{{classes.title}}" wx:if="{{title}}">{{title}}</view>
    <slot wx:else></slot>
    <view class="{{classes.bar}}" wx:if="{{current}}"></view>
</view>
