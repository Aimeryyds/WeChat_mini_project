<scroll-view class="wux-class {{classes.wrap}}" scrollX="{{direction==='horizontal'}}" scrollY="{{direction==='vertical'}}" wx:if="{{scroll}}">
    <slot></slot>
</scroll-view>
<view class="wux-class {{classes.wrap}}" wx:else>
    <slot></slot>
</view>
