<view catchtouchmove="handleTouchMove" class="inner" wx:if="{{visible}}">
    <image bindtap="onClickTarget" class="image" src="{{message.contentMetadata.components[0].config.src}}"></image>
    <view bindtap="handleClose" class="close">╳</view>
</view>
