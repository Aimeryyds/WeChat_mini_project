<view class="btn_area">
    <input focus bindconfirm="do_changename" bindinput="login_u" class="bind_input" maxlength="15" value="{{username}}"></input>
</view>
<button bindtap="changename" class="bind_btn" wx:if="{{allowadd}}">提交</button>
<button class="bind_btn graybg" wx:else>提交</button>
