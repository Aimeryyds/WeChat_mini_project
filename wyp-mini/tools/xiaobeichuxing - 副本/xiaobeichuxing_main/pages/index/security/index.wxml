<view class="page">
    <my-component innerText="安全用车提醒"></my-component>
    <scroll-view>
        <view class="img-list">
            <image src="{{img01}}" style="height:268px"></image>
            <image src="{{img02}}" style="height:390px"></image>
            <image src="{{img03}}" style="height:389px"></image>
            <image src="{{img04}}" style="height:388px"></image>
            <image src="{{img05}}" style="height:388px"></image>
            <image src="{{img06}}" style="height:387px"></image>
            <image src="{{img07}}" style="height:388px"></image>
            <image src="{{img08}}" style="height:431px"></image>
            <image src="{{img09}}" style="height:400px"></image>
            <image src="{{img10}}" style="height:84px"></image>
        </view>
    </scroll-view>
</view>
