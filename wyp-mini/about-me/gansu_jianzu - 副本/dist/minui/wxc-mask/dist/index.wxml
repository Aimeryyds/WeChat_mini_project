<view class="mask mask__status--{{status}}">
    <view bindtap="omMaskTap" class="mask__bg" style="background: rgba({{backgroundColor}},{{opacity}});"></view>
    <view class="mask__content mask__content--{{cAlign}}" style="{{__positionStyle}}">
        <slot></slot>
    </view>
</view>
