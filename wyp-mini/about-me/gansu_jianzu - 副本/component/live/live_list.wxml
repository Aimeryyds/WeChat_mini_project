<view class="live-list">
    <live-item bind:removefavorite="handleRemoveFavorite" id="liveItem_{{index}}" index="{{index}}" live="{{item}}" wx:for="{{liveList}}" wx:key="{{index}}"></live-item>
</view>
