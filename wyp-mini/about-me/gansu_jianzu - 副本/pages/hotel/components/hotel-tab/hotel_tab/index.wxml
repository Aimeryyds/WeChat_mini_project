<segements bindoptionchange="handleOptionChange" bindswitch="handleTabSwitch" class="Segements" filter="{{true}}" filterActive="{{filterActive}}" segements="{{segements}}">
    <filter-pop bindsubmit="handleFilter" filters="{{filters}}"></filter-pop>
</segements>
