<view style="height:0;overflow:hidden">
    <audio controls loop author="{{item.author}}" bindtap="play_audio" data-id="{{item.time_key}}" id="{{item.time_key}}" name="{{item.name}}" poster="{{item.imgurl}}" src="{{item.url}}"></audio>
</view>
