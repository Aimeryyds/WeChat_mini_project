<view class="form_tit" wx:if="{{f_item.title}}">{{f_item.title}}<text class="ture_color" wx:if="{{f_item.empty}}">*</text>
</view>
<view class="form_li">
    <textarea maxlength="{{f_item.maxlength?f_item.maxlength:140}}" name="{{f_item.name}}" placeholder="{{f_item.placeholder}}" value="{{f_item.value}}"></textarea>
</view>
