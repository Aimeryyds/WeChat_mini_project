<template name="icon">
    <view style="display: flex; align-items: center; justify-content: center; width: inherit; height: inherit;">
        <image src="{{icon}}" style="width: {{width}}; height: {{height}};"></image>
    </view>
</template>
