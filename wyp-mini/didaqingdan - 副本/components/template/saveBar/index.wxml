<view class="saveBtnWrapper" wx:if="{{$taroCompReady}}">
    <button bindtap="anonymousFunc0" class="saveBtn" wx:if="{{props.isAuth}}">{{saveText}}</button>
    <authorize-button compid="{{$compid__38}}" wx:else></authorize-button>
</view>
