<view class="{{styles.errorContainer}}" wx:if="{{$taroCompReady}}">
    <view class="{{styles.errorImage}}">
        <view class="{{'icon icon-error-placeholder '+styles.icon}}"></view>
    </view>
    <view class="{{styles.errorHint}}">{{errorHint}}</view>
    <button bindtap="funPrivatebizzz" class="{{styles.reTryBtn}}">重试</button>
</view>
