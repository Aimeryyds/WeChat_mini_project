<view class="{{styles.taskWrapper}}" wx:if="{{$taroCompReady}}">
    <view class="{{styles.checkBox}}"></view>
    <view class="{{styles.title}}">{{props.title}}</view>
    <view class="{{styles.tipWrapper}}">
        <view class="{{styles.date}}">{{dueDateInfo}}</view>
    </view>
</view>
