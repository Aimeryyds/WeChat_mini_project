<view class="{{styles.projectContainer}}" wx:if="{{$taroCompReady}}">
    <view class="{{styles.headerWrapper}}">
        <view class="{{styles.img}}" style="{{'background-image: url('+props.cover+')'}}"></view>
        <view class="{{styles.title}}">{{props.title}}</view>
        <view class="{{styles.desc}}">{{props.desc}}</view>
        <task-list compid="{{$compid__37}}"></task-list>
    </view>
</view>
