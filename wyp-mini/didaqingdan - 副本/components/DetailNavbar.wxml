<view class="navbar" wx:if="{{$taroCompReady}}">
    <view bindtap="toggleComplete" class="{{anonymousState__temp}}" wx:if="{{model.status===0}}"></view>
    <view bindtap="toggleComplete" class="icon icon-left icon-checkbox-completed" wx:else></view>
    <view bindtap="funPrivatebazzz" class="duedate">
        <text class="{{anonymousState__temp2}}">{{anonymousState__temp3}}</text>
    </view>
    <view bindtap="showMoreAction" class="icon icon-right icon-more"></view>
</view>
