<view bindtap="anonymousFunc0" class="{{anonymousState__temp}}" wx:if="{{$taroCompReady}}">
    <view class="icon icon-left icon-inbox" wx:if="{{item.name=='收集箱'}}"></view>
    <view class="icon icon-left icon-list" wx:else></view>
    <text class="title project-titles">{{item.name}}</text>
</view>
