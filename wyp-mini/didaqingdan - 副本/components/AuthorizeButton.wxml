<view class="{{className+'-container'}}" wx:if="{{$taroCompReady}}">
    <button bindgetuserinfo="anonymousFunc0" class="{{className+' auth-button'}}" openType="getUserInfo" wx:if="{{!hasShadow}}">{{buttonText}}</button>
    <shadow-button compid="{{$compid__20}}" wx:if="{{hasShadow}}"></shadow-button>
</view>
