<block wx:if="{{$taroCompReady}}">
    <error-page compid="{{$compid__488}}" wx:if="{{anonymousState__temp}}"></error-page>
    <view class="{{styles.templateContainer+' '+(isFullScreenMode?'full-screen':'')}}" wx:if="{{anonymousState__temp2}}">
        <main-container compid="{{$compid__489}}"></main-container>
        <template-footer></template-footer>
        <save-bar compid="{{$compid__490}}"></save-bar>
    </view>
</block>
