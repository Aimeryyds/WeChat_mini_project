<view class="container" wx:if="{{$taroCompReady}}">
    <view wx:for="{{loopArray25}}" wx:for-index="_anonIdx2" wx:key="id">
        <group-item compid="{{item[$compid__26]}}" wx:if="{{item[$original].isGroup}}"></group-item>
        <project-item compid="{{item[$compid__27]}}" wx:else></project-item>
    </view>
</view>
