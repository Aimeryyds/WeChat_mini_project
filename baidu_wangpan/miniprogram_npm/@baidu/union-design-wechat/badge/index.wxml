<view class="u-class u-badge">
    <slot></slot>
    <view class="u-badge-dot" wx:if="{{dot}}"></view>
    <view class="u-badge-count u-class-alone" wx:elif="{{count!==0}}">{{finalCount}}</view>
</view>
