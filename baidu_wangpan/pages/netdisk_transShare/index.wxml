<block wx:if="{{showNav}}">
    <Navbar></Navbar>
    <NavbarPlaceholder></NavbarPlaceholder>
</block>
<TransShare surl="{{surl}}" title="{{title}}" wx:if="{{isValid}}"></TransShare>
<Toast id="toast"></Toast>
<ShowDialog id="showDialog"></ShowDialog>
