<view class="sub-container" wx:if="{{showSub&&!closed}}">
    <view class="sub-desc">订阅后随时接收该分享的更新</view>
    <view bindtap="handleSub" class="sub-action">去订阅</view>
    <image bindtap="handleClose" class="sub-close" src="{{closeIcon}}"></image>
</view>
