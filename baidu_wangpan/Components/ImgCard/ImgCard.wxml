<view class="img-card-view">
    <image bindtap="_click" class="img-view" mode="widthFix" src="{{src}}"></image>
    <view class="img-info">
        <text>{{showName}}·{{size}}</text>
    </view>
</view>
