<view bindtap="actClick" class="activity activity-class" style="background-image:url('{{activity.showImg}}'); bottom: {{bottom}}; top: {{top}}" wx:if="{{show&&innerShow}}">
    <view catchtap="actCancel" class="cancel" wx:if="{{showCancel}}"></view>
</view>
