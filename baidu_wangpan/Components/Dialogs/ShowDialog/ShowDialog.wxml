<Dialog bind:cancel="cancel" bind:confirm="confirm" btnCancelColor="{{cancelColor}}" btnConfirmColor="{{confirmColor}}" cancelValue="{{cancelText}}" confirmValue="{{confirmText}}" id="dialog" isHideCancel="{{!showCancel}}" title="{{title}}">
    {{content}}
</Dialog>
