<view catchtap="mediaTap" class="media-wrapper">
    <image class="media-icon" mode="aspectFill" src="{{chlicon}}" wx:if="{{chlicon}}">
        <view class="mask"></view>
    </image>
    <view class="media-name">
        <text>{{chlname}}</text>
    </view>
    <view class="pub-time" wx:if="{{publishTime}}">
    · {{publishTime}}
  </view>
</view>
