<view class="exclusive-media-wrapper">
    <view class="title">有趣的人值得关注</view>
    <scroll-view>
        <view class="media-wrapper" style="width:{{list.length*286+24}}rpx">
            <media chlicon="{{item.chlicon}}" chlid="{{item.chlid}}" chlname="{{item.chlname}}" desc="{{item.desc}}" follow="{{item.follow}}" wx:for="{{list}}" wx:key="chlid"></media>
        </view>
    </scroll-view>
</view>
