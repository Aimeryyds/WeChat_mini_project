<xxw-bg-loading bindrefresh="render" status="{{status}}" wx:if="{{status!==pageStatus.ready}}"></xxw-bg-loading>
<swiper-scroll-view enablePulldownRefresh bindpullDownRefresh="onPullDownRefresh" bindreachBottom="onReachBottom" bindretry="onReachBottom" bindscroll="onScroll" bottomLoadingStatus="{{pullUpStatus}}" finishText="{{loadDataTips}}" hasMore="{{hasMore}}" pulldownFinished="{{!pulldownPending}}" scrollTop="{{scrollTop}}" wx:else>
    <recommend-item articleId="{{item.articleId}}" articleList="{{item.articleList}}" atype="{{item.atype}}" chlname="{{item.chlname}}" commentCount="{{item.commentCount}}" hasRead="{{item.hasRead}}" isFixedTop="{{item.showType!==8}}" showType="{{item.showType}}" title="{{item.title}}" zwbFolded="{{hasPullDown}}" zwbHasExposured="{{item.zwbHasExposured}}" wx:for="{{fixedTopList}}" wx:key="_uid"></recommend-item>
    <block wx:for="{{service.reverse(pullDownList)}}" wx:for-index="page" wx:for-item="firstItem" wx:key="_uid">
        <recommend-item _uid="{{item._uid}}" articleId="{{item.articleId}}" atype="{{item.atype}}" bindtapDislike="tapDisLike" chlicon="{{item.chlicon}}" chlid="{{item.chlid}}" chlname="{{item.chlname}}" commentCount="{{item.commentCount}}" data-index="{{index}}" data-page="{{page}}" data-source="pullDownList" durationStr="{{item.durationStr}}" hasRead="{{item.hasRead}}" hideSplitLine="{{item.hideSplitLine}}" hotComment="{{item.hotComment}}" imgBig="{{item.imgBig}}" imgCount="{{item.imgCount}}" imgSmall="{{item.imgSmall}}" imgThree="{{item.imgThree}}" intro="{{item.intro}}" isExclusive="{{item.isExclusive}}" isVideo="{{item.isVideo}}" joinCount="{{item.joinCount}}" playCount="{{item.playCount}}" posterShareImage="{{item.posterShareImage}}" publishTime="{{item.publishTime}}" reasonFlag="{{item.reasonFlag}}" showType="{{item.showType}}" title="{{item.title}}" topicId="{{item.topicId}}" topicName="{{item.topicName}}" vid="{{item.vid}}" voteId="{{item.voteId}}" weiboList="{{item.weiboList}}" wx:for="{{firstItem.list}}" wx:key="_uid"></recommend-item>
        <view bindtap="tapRefresh" class="refresh-item-wrapper" wx:if="{{page===0}}">
            <view>上次看到这里，点击刷新</view>
            <image class="refresh-icon" src="http://mat1.gtimg.com/www/js/news/refresh.png"></image>
        </view>
    </block>
    <block wx:for="{{pullUpList}}" wx:for-index="page" wx:for-item="firstItem" wx:key="_uid">
        <recommend-item _uid="{{item._uid}}" articleId="{{item.articleId}}" atype="{{item.atype}}" bindtapDislike="tapDisLike" chlicon="{{item.chlicon}}" chlid="{{item.chlid}}" chlname="{{item.chlname}}" commentCount="{{item.commentCount}}" data-index="{{index}}" data-page="{{page}}" data-source="pullUpList" durationStr="{{item.durationStr}}" hasRead="{{item.hasRead}}" hideSplitLine="{{item.hideSplitLine}}" hotComment="{{item.hotComment}}" imgBig="{{item.imgBig}}" imgCount="{{item.imgCount}}" imgSmall="{{item.imgSmall}}" imgThree="{{item.imgThree}}" intro="{{item.intro}}" isExclusive="{{item.isExclusive}}" isExclusiveMedia="{{item.isExclusiveMedia}}" isVideo="{{item.isVideo}}" joinCount="{{item.joinCount}}" mediaList="{{item.mediaList}}" playCount="{{item.playCount}}" posterShareImage="{{item.posterShareImage}}" publishTime="{{item.publishTime}}" reasonFlag="{{item.reasonFlag}}" showType="{{item.showType}}" title="{{item.title}}" topicId="{{item.topicId}}" topicName="{{item.topicName}}" vid="{{item.vid}}" voteId="{{item.voteId}}" weiboList="{{item.weiboList}}" wx:for="{{firstItem.list}}" wx:key="_uid"></recommend-item>
    </block>
</swiper-scroll-view>
<wxs module="service" src="service.wxs" />