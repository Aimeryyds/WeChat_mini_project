<ScheduleList apiMatches="{{schedule.apiMatches}}" dateDesc="{{schedule.dateDesc}}" dates="{{schedule.dates}}" emptyStr="没有预约比赛" scrollHeight="{{contentHeight}}" wx:if="{{schedule.isFetched}}"></ScheduleList>
