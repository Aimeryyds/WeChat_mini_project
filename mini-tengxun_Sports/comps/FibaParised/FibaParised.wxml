<view animation="{{animationLsi}}" class="m-parised">
    <image class="icon icon-parsied" src="/assets/FIBA/icon_good_pop.png"></image>
    <view class="sponsor">
        <image class="logo" src="{{logo}}" wx:if="{{logo}}"></image>
        {{content}}
    </view>
</view>
