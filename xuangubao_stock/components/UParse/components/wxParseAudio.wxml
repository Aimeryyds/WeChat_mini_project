<audio controls author="{{node.attr.author}}" class="{{[node.classStr]}}" id="{{node.attr.id}}" loop="{{node.attr.loop}}" name="{{node.attr.name}}" poster="{{node.attr.poster}}" src="{{node.attr.src}}" style="{{node.styleStr}}"></audio>
