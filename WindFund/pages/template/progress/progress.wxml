<view class="fullscreen" wx:if="{{showProgress}}"></view>
<view class="processbox" wx:if="{{showProgress}}">
    <view class="process-text">下载进度：{{progress}}%</view>
    <view class="process-style">
        <view style="width:{{progress}}%;"></view>
    </view>
</view>
